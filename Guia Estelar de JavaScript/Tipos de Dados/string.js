// Tipos de Dados JS:

// STRING: fica dentre aspas, simples ou duplas ou crase.

	var sou = "I'm" //"" utilizado geralmente quando vai ter umas aspas simples dentro para especificar algo;
	var nome = 'KENNEDY' //'' é uzada para strings pequenas e que não tem algo com aspas especificadas em seu anterior;
	
	var texto = `${sou}, 
	${nome}` // `` é usado em casos que tem experssões dentro ou até mesmo para quebrar linhas pois dá a possiblidade do 
	//código em bloco.

	console.log(`${texto}`)